apiVersion: v1
kind: Service
metadata:
  name: voting-app
  labels:
    app: voting-app

spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 31000
      name: "vote-service"
  selector:
    app: vote